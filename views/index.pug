<!DOCTYPE html>
html(lang="en")
    head
        meta(charset='UTF-8')
        title Flower Land
        link(rel="stylesheet", href="/css/main.css")

    body
        header 
            h1 Flower Land
        

        main 
            form(action="/store " method="POST")
            label(for="flower") Enter flower 
            input(type="text", id="flower" name="flower") 
            button(type="submit") Search Flower